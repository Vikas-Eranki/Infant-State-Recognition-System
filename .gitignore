# ── Large data files (regenerated on Kaggle) ─────────────────────────────────
data/processed/audio_clean/
data/processed/X.npy
data/processed/y.npy
data/raw/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.env
.ipynb_checkpoints/

# ── Virtual environments ──────────────────────────────────────────────────────
venv/
env/
.venv/

# ── OS files ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDE ───────────────────────────────────────────────────────────────────────
.vscode/
.idea/

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log

# ── Model weights and checkpoints (Phase 2/3, too large for GitHub) ──────────
*.h5
*.keras
*.tflite
*.pb
*.ckpt
*.pth
checkpoints/
saved_models/

# ── Temporary conversion files (created during preprocessing, auto-deleted) ───
data/temp_converted/

# ── Jupyter runtime ──────────────────────────────────────────────────────────
.jupyter/
*.ipynb_checkpoints
```

---

## What Gets Committed vs Ignored — Final Picture

**Committed to GitHub (small, essential):**
```
data/processed/clean_manifest.csv       (manifest for split)
data/processed/preprocessing_params.json
data/processed/label_encoder.json
data/processed/augmentation_log.csv
reports/ablation_table.csv              ← Artifact 01
reports/confusion_matrix_svm.png
reports/failure_analysis_svm.png
reports/failure_analysis_pairs.png
reports/failure_analysis_breakdown.png
reports/spectrograms_per_class.png
reports/class_distribution_post_preprocessing.png
reports/waveform_vs_spectrogram_hungry_bellypain.png
reports/confused_pairs_spectrograms.png
reports/latex/phase1_report.tex
```

**Gitignored (large, regenerated from notebooks):**
```
data/raw/                    (234 MB — Kaggle download)
data/processed/audio_clean/  (451 MB — regenerated by 02_preprocessing)
data/processed/X.npy         (206 MB — regenerated by 03_feature_engineering)
data/processed/y.npy         (5.5 KB — small but regenerated alongside X.npy)
data/temp_converted/         (auto-deleted by preprocessing notebook)